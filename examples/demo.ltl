// examples/demo.ltl — runnable demo for ltl_du_plus

import std::io
use ltl_du_plus as lib

fn main() {
    io::println("ltl_du_plus demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}