// ltl-du-plus/main.ltl
//
// Parallel disk-usage scanner with interactive TUI.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-du-plus v0.1.0")
    io::println("Parallel disk-usage scanner with interactive TUI.")
    0
}
